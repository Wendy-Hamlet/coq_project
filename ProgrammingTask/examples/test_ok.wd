# Valid test program
# Demonstrates all major language features

int x;
int y;
int z;

x = 10;
y = 20;

z = x + y * 2;

int* p;
p = &x;

int val;
val = *p;

if (x < y) then {
    z = x + 1
} else {
    z = y + 1
};

while (x > 0) do {
    x = x - 1
};

int cond;
cond = (x == 0) && (y > 10);

int neg;
neg = !cond;

*p = 42